1 + 1
1 + '1'
1 + a

true() and true()
true() and 's'
true() and a

fn()[true()]
fn()[1]
fn()[2]
fn()[last()]

$var[true()]
$var[1]
$var[2]
$var[last()]

count(a)
count(1)
count(fn())
count($var)

number(a)
number(1)
number(fn())
number($var)

lang(a)
lang(1)
lang(fn())
lang($var)

local-name()
local-name(.)
local-name(*)

name()
name(.)
name(*)

namespace-uri()
namespace-uri(.)
namespace-uri(*)

a | fn() | $var
(a | fn() | $var)[1]
(a | fn() | $var)[2]
(a | fn() | $var)[last()]

(a[1])[last()]
a[true()][1][true()][1][2][true()][last()][true()]

a[position()=1]
a[position()=100]
a[position()=last()]

a[.//b]

not(true())
not(1)
not(boolean(boolean(a)))

0 < 1
'3.0' >= '5.2'
1 > true()
4 <= 'abc'

name() = 'abc'
position() != 1
2 = last()
'x' != local-name()

(a | fn()[1] | $var[2])[1]
(a | fn()[1] | $var)[1]
(ancestor::d:tgroup|ancestor::d:entrytbl)[position() = last()]
